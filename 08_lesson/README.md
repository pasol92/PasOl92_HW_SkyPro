# Тесты для API Yougile

## Настройка перед запуском

Перед запуском тестов необходимо указать:

1. **[Base URL API](https://ru.yougile.com/api-v2)** — в фикстуре `base_url` в файле `conftest.py`.
2. **"+ZB7GqHvHNQtq7Ag7MELE7o9LpPLvdYt7f88kgxN6lsSWbncKx0NpbNyzuol+PTv"** — в фикстуре `auth_token` в файле `conftest.py`.

## Установка зависимостей

```bash
pip install pytest requests
